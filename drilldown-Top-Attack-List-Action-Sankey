SELECT from_itime(itime) AS timestamp, attack, srcip, dstip, action FROM
###(
SELECT itime,
  attack,
  srcip, 
  dstip, 
  action
FROM $log 
WHERE $filter-exclude-var
ORDER BY itime desc
)### t
WHERE $filter-drilldown
ORDER BY timestamp desc
